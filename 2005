#include <iostream>
#include <map>
#include <vector>
#include <string>
#include <set>
#include <queue>

//using namespace std;
//
//int main()
//{
//	setlocale(LC_ALL, "Rus");
//	int k = 0;
//	string n;
//	map<char, int> m;
//	vector<string> s = { "ПАРК","КАРП","СОН" ,"НОС" ,"ДЕНЬ" };
//	queue <string> que;
//	set<map<char, int>> set;
//	for (int i = 0; i != size(s); i++)
//	{
//		que.push(s[i]);
//	}
//	while (!que.empty())
//	{
//		n = que.front();
//		que.pop();
//		m.clear();
//		k = size(set);
//		for (int j = 0; j != n.size(); j++)
//		{
//			++m[n[j]];
//		}
//		set.insert(m);
//		if (size(set) > k) {
//			cout << n << " ";
//		}
//	}
//}

//{ 2, 6, 4, 8, 12, 74, 23, 14 }

using namespace std;

//int main()
//{
//	setlocale(LC_ALL, "Rus");
//	queue <int> st0;queue <int> st1;queue <int> st2; queue <int> stf;
//	int dl = 0,dl1,el;
//	cout << "введите колличество чисел: ";
//	cin >> dl;
//	cout << endl;
//	for (int i = 0; i < dl; i++)
//	{
//		cin >> el;
//		st0.push(el);
//	}
//	cout << st0.size();
//	dl1 = dl; st1 = st0; int sred = 0;
//	while (!(st1.empty()))
//	{
//		sred += st1.front();
//		st1.pop();
//	}
//	sred = sred / dl; st1 = st0;
//	stf.push(sred);
//	while (!(st1.empty()))
//	{
//		st2.push(st1.front());
//		st1.pop();
//		
//	}
//	st2 = st0;
//	while (!(st2.empty()))
//	{
//		st2.front();
//		st2.pop();
//	}
//	return(0);
//}
int main()
{
	setlocale(LC_ALL, "Rus");
	queue <int> st0; queue <int> st1;
	int dl = 0,el,sred=0;
	cout << "введите колличество чисел: ";
	cin >> dl;
	cout << endl;
	for (int i = 0; i < dl; i++)
	{
		cin >> el;
		st0.push(el);
	}
	cout << " / " << st0.size()<< endl;
	while (!(st0.empty()))
	{
		st1.push(st0.front());
		sred += st0.front();
		st0.pop();
	}
	sred = sred / (st1.size());
	st0.push(sred);
	while (!(st1.empty()))
	{

		st0.push(st1.front());
		st1.pop();
	}
	while (!(st0.empty()))
	{
		cout << st0.front() << " ";
		st0.pop();
	}
}
